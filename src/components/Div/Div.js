// import Button from "@restart/ui/esm/Button";

function LocalDiv({children,type,className,id},props) {


    return(
        <div className={className} id={id}>
            {children}
        </div>
    )
    }
    export default LocalDiv;